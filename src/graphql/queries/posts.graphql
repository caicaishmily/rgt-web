query Posts($cursor: String, $limit: Int!) {
  posts(limit: $limit, cursor: $cursor) {
    posts {
      id
      creatorId
      createdAt
      updatedAt
      title
      text
      textSnippet
      points
    }
    hasMore
  }
}
